package ru.job4j.dop;

public class Dop1 {
    public int[] sort(int[] array) {
        int[] a = {};
        for (int i = 0; i < a.length - 1; i++)
            if (a[i] < a[i + 1])
                break;
        return array;
    }
}